import { msg } from "./i18n";

/**
 * 데모 데이터가 값으로 들고 있는 "정해진 어휘".
 *
 * 사용자가 쓴 글·메시지·방 제목은 쓴 사람의 언어 그대로 보여야 하지만,
 * 언어 이름·레벨·상대 시간처럼 종류가 정해진 값은 화면 문구에 가깝습니다.
 * 이 목록을 사전에 등록해 두면 화면에서 `tx(post.language)` 처럼 부를 때 번역됩니다.
 * 목록에 없는 값은 tx() 가 원문을 그대로 통과시키므로 자유 텍스트는 건드리지 않습니다.
 *
 * 데모 데이터에 새 어휘를 넣으면 여기에도 추가하세요 — tests/i18n.test.mjs 가
 * 이 목록이 모두 사전에 있는지 검사합니다.
 */
export const DATA_VOCAB = [
  // 언어
  msg("한국어"),
  msg("영어"),
  msg("일본어"),
  msg("스페인어"),
  msg("프랑스어"),
  msg("독일어"),
  // 국가 — 서버가 프로필에 담아 보내는 값
  msg("대한민국"),
  // 레벨
  msg("모든 레벨"),
  // 상대 시간
  msg("방금"),
  msg("2분 전"),
  msg("3분 전"),
  msg("4분 전"),
  msg("5분 전"),
  msg("8분 전"),
  msg("10분 전"),
  msg("12분 전"),
  msg("27분 전"),
  msg("32분 전"),
  msg("1시간 전"),
  msg("2시간 전"),
  msg("어제"),
  msg("2일 전"),
  msg("3일 전"),
  // 대화 목록의 짧은 시간 표기 (전 없이)
  msg("2분"),
  msg("38분"),
  msg("1시간"),
] as const;
